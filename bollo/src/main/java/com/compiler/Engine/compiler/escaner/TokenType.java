package com.compiler.Engine.compiler.escaner;

public enum TokenType {
    //PALABRAS RESERVADAS
    AUTO, BREAK, CASE, CHAR, CONST, CONTINUE, DEFAULT, DO, DOUBLE,
    ELSE, ENUM, EXTERN, FLOAT, FOR, GOTO, IF, INT, LONG, REGISTER,
    RETURN, SHORT, SIGNED, SIZEOF, STATIC, STRUCT, SWITCH, TYPEDEF,
    UNION, UNSIGNED, VOID, VOLATILE, WHILE, PACKED, INCLUDE,

    //OPERADORES
    MUL, DIV, MOD, PLUS, MINUS, ASSIGN,
    SHL, SHR,
    LT, GT, LE, GE, EQ, NE,
    AND, OR, XOR, LAND, LOR,
    COMMA, NOT, INC, DEC, MINUS_ASSIGN, PLUS_ASSIGN, MUL_ASSIGN, 
    DIV_ASSIGN, MOD_ASSIGN,

    //PUNTUACION
    LBRACE, RBRACE, LBRACKET, RBRACKET,
    LPAREN, RPAREN, SEMICOLON, HASH,
    BACKSLASH, APOSTROPHE, LINE_COMMENT, LBLOCK_COMMENT, RBLOCK_COMMENT,

    IDENTIFIER, NUMBER, REAL, EOF, STRING,

    UNKNOWN

}
